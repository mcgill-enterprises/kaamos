{
  "name": "@kafka-tutorial/torakka",
  "version": "1.0.0",
  "description": "State service for the Helka Expanse fuel economy. Consumes Kafka events and persists to CockroachDB.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch --import tsx/esm src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kafka-tutorial/shared": "*",
    "kafkajs": "^2.2.4",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.19.2",
    "@types/node": "^20.0.0"
  }
}
